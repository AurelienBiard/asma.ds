# Composant PIN

> Voir **`components/code-otp/code-otp-component-spec.md`** — PIN et Code-OTP partagent la même brique atomique (`OTP-cell`) et sont documentés ensemble dans ce fichier unique pour éviter la duplication.

Résumé rapide : PIN = 4 instances `OTP-cell` masquées (`•`), contre 6 instances visibles pour Code-OTP.
